spring:
  datasource:
    url: jdbc:postgresql://delivery_db:5432/300logistics
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true